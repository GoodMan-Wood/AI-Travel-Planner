# AI Travel Planner Frontend

Next.js 14 application providing the web experience for the AI Travel Planner.

## Development

```bash
npm install
npm run dev
```

Configure environment variables via `.env` based on `.env.example` before running the dev server.
