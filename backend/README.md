# AI Travel Planner Backend

FastAPI service powering itinerary generation, budgeting, and integrations for the AI Travel Planner project.

## Getting Started

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

Set environment variables based on `.env.example` before running the service.
